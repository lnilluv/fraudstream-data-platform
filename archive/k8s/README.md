# Kubernetes Archive

These manifests are archived reference material and are not the active deployment path.

For current VPS deployment, use `deploy/compose/docker-compose.yml`.

All credential values in archived manifests must remain placeholders.
